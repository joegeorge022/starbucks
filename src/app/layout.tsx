import type { Metadata } from "next";
import { Inter, Poppins } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const poppins = Poppins({
  variable: "--font-poppins",
  weight: ["300", "400", "500", "600", "700"],
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Starbucks (Vazha Edition) - Brewed for the bold. Inspired by you.",
  description: "This isn't your average Starbucks. This is Starbucks, reimagined through a banana leaf filter. Brewed for the bold. Inspired by you.",
  keywords: "Starbucks, Vazha, Banana, Coffee, Kerala, Malayalam, Pazham Pori",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${poppins.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
